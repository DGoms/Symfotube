<nav class="navbar navbar-dark sticky-top bg-dark flex-md-nowrap p-0">
    <a class="navbar-brand col-sm-3 col-md-2 mr-0" href="{{ path('home') }}">Symfotube</a>
    <span class="form-group p-0 m-0 w-100">
        <input class="form-control form-control-dark " type="text" placeholder="Search" aria-label="Search">
    </span>
    <ul class="navbar-nav px-3">
        <li class="nav-item text-nowrap">
            {% if app.user %}
                <a class="nav-link" href="{{ path('fos_user_security_logout') }}">Log out</a>
            {% else %}
                <a class="nav-link" href="{{ path('fos_user_security_login') }}">Log in</a>
            {% endif %}
        </li>
    </ul>
</nav>