{% extends 'AppBundle::body.html.twig' %}

{% block title %}New video - {% endblock %}

{% block content %}
        <div class="col-md-12">
            {{ form_start(form, {attr: {novalidate: 'novalidate'}}) }}
            {{ form_widget(form) }}
                <button class="btn btn-lg btn-primary btn-block btn-raised" type="submit">Publish</button>
            {{ form_end(form) }}
        </div>
{% endblock %}
