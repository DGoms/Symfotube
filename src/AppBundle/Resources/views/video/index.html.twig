{% extends 'AppBundle::base.html.twig' %}

{% block content %}
    {% if users is defined %}
        {{ render(controller('AppBundle:User:listUsers', { 'users': users })) }}
    {% endif %}
    {% if videos is defined %}
        {{ render(controller('AppBundle:Video:listVideos', { 'videos': videos })) }}
    {% endif %}
{% endblock %}
