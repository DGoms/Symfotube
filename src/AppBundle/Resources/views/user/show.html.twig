{% extends '@App/body.html.twig' %}

{% block title %}{{ user.username }} | {% endblock %}

{% block content %}
    <div id="profile-header" class="row">
        <!-- thumbnail -->
        <div class="col-md-2 offset-2">

        </div>

        <!-- info -->
        <div class="col-md-6">
            <h1>{{ user.username }}</h1>
            <h3>{{ user.firstName ~ ' ' ~ user.lastName }}</h3>
        </div>
    </div>
{% endblock %}